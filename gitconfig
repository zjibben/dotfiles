[user]
	name = Zach Jibben
	email = zjibben@gmail.com
[credential]
	helper = cache --timeout=300
[commit]
        gpgsign = true
[pull]
        ff = only
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[lfs]
	cachecredentials = true
[protocol "file"]
	allow = always
